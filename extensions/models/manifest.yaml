manifestVersion: 1
name: "@mgreten/ntfy-notify"
version: "2026.08.23.1"
description: >
  Send push notifications via ntfy.sh (self-hosted or public), directly or
  through the generic notification-outbox transport contract. Supports title,
  message, priority, optional emoji tags, and actions.

repository: https://github.com/meagerfindings/swamp-ntfy-notify

models:
  - ntfy_notify.ts

additionalFiles:
  - README.md
  - LICENSE.txt

platforms: []
